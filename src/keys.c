#include "../inc/cub3D.h"
#include "../libft/libft.h"
#include "../mlx_linux/mlx.h"
#include <stdlib.h>

int	key_press(int key)
{
	if (key == ESC)
	{	
		exit(0);
	}
	return (0);
}

int	x_press(void)
{
	exit(0);
}